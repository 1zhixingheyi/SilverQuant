# SilverQuant 系统整体架构图

## 1. 系统分层架构（清晰版）

```plantuml
@startuml
!theme plain
skinparam defaultFontName "Microsoft YaHei"
skinparam defaultFontSize 14
skinparam classFontSize 12
skinparam packageStyle rectangle

title SilverQuant 系统分层架构

' 顶层 - 用户层
package "用户配置层" {
  [策略配置 run_*.py] as Config
  [凭证管理 credentials.py] as Cred
}

' 第二层 - 策略层
package "策略执行层" {
  [策略调度器 APScheduler] as Scheduler
  [买入策略 BaseBuyer] as Buyer
  [卖出策略 BaseSeller] as Seller
  [选股器 Selector] as Selector
}

' 第三层 - 服务层
package "核心服务层" {
  [交易代理 BaseDelegate] as Delegate
  [数据订阅 BaseSubscriber] as Subscriber
  [缓存管理 UtilsCache] as Cache
}

' 第四层 - 数据层
package "数据访问层" {
  [AKShare] as AKShare
  [Tushare] as Tushare
  [QMT数据] as QMT
  [问财AI] as Wencai
}

' 底层 - 交易层
package "交易执行层" {
  [QMT实盘] as QMTTrade
  [掘金模拟] as GMTrade
  [回测引擎] as Backtest
}

' 主要数据流向
Config --> Scheduler : 启动
Scheduler --> Selector : 选股
Scheduler --> Buyer : 买入
Scheduler --> Seller : 卖出

Cred --> Delegate : 配置

Selector --> Wencai : AI选股
Subscriber --> AKShare : 数据
Subscriber --> Tushare : 数据
Subscriber --> QMT : 行情

Buyer --> Delegate : 买入指令
Seller --> Delegate : 卖出指令

Delegate --> QMTTrade : 实盘
Delegate --> GMTrade : 模拟
Scheduler --> Backtest : 回测

Cache --> Subscriber : 缓存
Cache --> Selector : 缓存

@enduml
```

## 2. 系统组件关系图（简化版）

```plantuml
@startuml
!theme plain
skinparam defaultFontName "Microsoft YaHei"
skinparam defaultFontSize 14
skinparam classFontSize 12

title SilverQuant 系统组件关系

' 中央核心
package "核心系统" {
  (SilverQuant\n量化交易系统) as System
}

' 配置模块
package "配置管理" #LightGreen {
  [credentials.py\n交易凭证] as CredFile
  [run_*.py\n策略配置] as StrategyFile
}

' 策略模块
package "策略模块" #LightBlue {
  [APScheduler\n任务调度] as Scheduler
  [BaseBuyer\n买入策略] as Buyer
  [BaseSeller\n卖出策略] as Seller
}

' 数据模块
package "数据模块" #LightYellow {
  [BaseSubscriber\n数据订阅] as Subscriber
  [多数据源\nAKShare/Tushare/QMT] as DataSources
  [UtilsCache\n缓存管理] as Cache
}

' 交易模块
package "交易模块" #LightPink {
  [BaseDelegate\n交易代理] as Delegate
  [QMT\n实盘交易] as QMTTrade
  [掘金\n模拟交易] as GMTrade
}

' 选股模块
package "选股模块" #Lavender {
  [Selector\n股票筛选] as Selector
  [问财AI\n智能选股] as WencaiAI
}

' 连接关系
CredFile --> System : 凭证配置
StrategyFile --> Scheduler : 策略参数

Scheduler --> System : 调度控制
Buyer --> Delegate : 买入请求
Seller --> Delegate : 卖出请求

Subscriber --> DataSources : 获取数据
Cache --> System : 数据缓存

Delegate --> QMTTrade : 实盘执行
Delegate --> GMTrade : 模拟执行

Selector --> WencaiAI : AI选股
Selector --> System : 选股结果

System --> Cache : 存储数据
System --> Delegate : 交易指令

@enduml
```

## 3. 数据流转示意图

```plantuml
@startuml
!theme plain
skinparam defaultFontName "Microsoft YaHei"
skinparam defaultFontSize 14

title SilverQuant 数据流转示意

left to right direction

' 数据源
database "数据源" {
  [AKShare] as AK
  [Tushare] as TS
  [QMT行情] as QMT
  [问财AI] as WC
}

' 数据处理
package "数据处理" {
  [数据订阅器] as Sub
  [缓存系统] as Cache
  [数据验证] as Valid
}

' 策略执行
package "策略执行" {
  [选股器] as Sel
  [买入策略] as Buy
  [卖出策略] as Sell
}

' 交易系统
package "交易系统" {
  [交易代理] as Trade
  [实盘接口] as Real
  [模拟接口] as Sim
}

' 主要流程
AK --> Sub : 历史数据
TS --> Sub : 基础数据
QMT --> Sub : 实时行情

Sub --> Cache : 存储数据
Sub --> Valid : 数据清洗
Cache --> Sel : 历史回测

WC --> Sel : AI选股
Sel --> Buy : 选股结果
Valid --> Sell : 实时数据

Buy --> Trade : 买入指令
Sell --> Trade : 卖出指令

Trade --> Real : 实盘交易
Trade --> Sim : 模拟交易

@enduml
```

## 4. 模块职责说明

### 用户接口层
- **策略配置文件** (`run_*.py`): 各种策略的入口点，配置买卖参数
- **凭证配置** (`credentials.py`): 存储交易账户、API密钥等敏感信息
- **缓存目录** (`_cache/`): 存储历史数据、持仓信息、交易记录等

### 策略执行层
- **策略调度器** (APScheduler): 定时执行选股、买卖扫描任务
- **买入策略** (BaseBuyer): 管理买入逻辑，包括仓位控制、风险限制
- **卖出策略** (BaseSeller): 管理卖出逻辑，组合多种卖出组件
- **股票池管理** (StocksPool): 维护可交易股票范围，过滤黑名单
- **选股器** (Selector): 根据技术指标或AI模型筛选目标股票

### 核心服务层
- **交易代理** (BaseDelegate): 抽象交易接口，支持多交易端
- **数据订阅** (BaseSubscriber): 实时获取行情数据，支持多种数据源
- **缓存管理** (UtilsCache): 管理数据缓存，减少API调用频率
- **通知服务** (DingMessager): 钉钉机器人消息推送

### 数据访问层
- **AKShare**: 获取A股历史和实时数据
- **Tushare**: 备用数据源，需要token
- **通达信TDX**: 本地数据源，支持历史数据回放
- **MiniQMT**: QMT自带的行情数据
- **同花顺问财**: AI选股数据源

### 交易执行层
- **QMT实盘** (xtquant): 连接券商QMT进行实盘交易
- **掘金模拟盘** (gm_delegate): 掘金平台模拟交易
- **回测引擎** (daily_backtest): 历史数据回测功能

## 5. 数据流向说明

### 配置流程
1. 加载 `credentials.py` 配置文件
2. 初始化交易账户和API密钥
3. 加载策略参数配置

### 数据流程
1. 订阅多个数据源获取行情
2. 数据清洗和验证
3. 存储到缓存系统
4. 提供给策略使用

### 策略流程
1. 定时触发选股任务
2. AI或技术指标筛选股票
3. 生成买入/卖出信号
4. 风险控制和仓位管理

### 交易流程
1. 接收策略交易指令
2. 通过交易代理下单
3. 实盘或模拟执行
4. 记录交易结果

## 6. 关键设计特点

### 分层解耦
- 每一层只与相邻层交互，降低耦合度
- 统一接口设计，便于扩展和替换组件

### 组件化策略
- 买入、卖出、选股可独立配置
- 支持组合多个卖出组件，实现灵活风控

### 多数据源容错
- 主数据源失效时自动切换备用源
- 本地缓存保证离线时基本功能可用

### 实时监控
- 钉钉推送交易通知和异常报警
- 本地日志记录详细操作历史